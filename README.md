# xk6-subcommand-httpd

> httpbin inside k6

A k6 extension that adds a new `httpbin` subcommand to k6, providing a fully functional, local HTTP request and response service right inside your load testing tool.

Use it for zero-setup, local API testing, debugging k6 scripts, and ensuring predictable behavior in CI/CD pipelines.
